# Get a token at: https://github.com/settings/tokens
# Scopes needed: none (public repo search only)
# Without a token, rate limit is 10 requests/minute
VITE_GITHUB_TOKEN=your_personal_access_token_here
